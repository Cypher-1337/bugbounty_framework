/// <reference types="react" />
import { Store } from '../../hooks/useStore';
export declare const Menu: ({ queues, selectedStatuses, }: {
    queues: string[] | undefined;
    selectedStatuses: Store['selectedStatuses'];
}) => JSX.Element;
